# Qwen2.5 微调学习笔记（小白版）

> 记录时间：2026-09-06
> 记录对象：`qwen2.5-finetune` 项目（Qwen2.5 电商客服微调）
> 面向读者：微调实战从零到进阶。前半部分用生活类比入门，后半部分为技术版指标详解与结果记录。
> 当前进度：✅ 环境搭建 ✅ 正式训练 ✅ 对比测试 ✅ 3-epoch 实验 ✅ 合并权重 ✅ 对话实测 ✅ 大模型盲评（胜率 92.4% 基线）→ **阶段验收通过**；升级路线（全量数据 / 1.5B）见 §5 备用

---

## 0. 一句话总结

我们在做的事是：**给一个"聪明但没经验"的通用 AI 模型上"电商客服培训课"，让它学会像淘宝客服一样说话**。

整个过程中我们完成了：搭好训练环境 → 下载模型 → 用 3 条测试数据跑通流程，证明这台电脑能干活了。

---

## 1. 背景知识：先用生活类比搞懂几个词

| 术语 | 生活类比 | 人话解释 |
|---|---|---|
| **大语言模型** | 一个读过很多书的"通才实习生" | 会聊天、会写东西，但没专门练过某个行业的话术 |
| **底座模型** | 实习生本人 | 本项目用的底座是 Qwen2.5-0.5B，一个 4.94 亿参数的小模型 |
| **微调（Fine-tuning）** | 把实习生送去"客服岗前培训" | 用大量客服对话让它学会客服的语气、话术和业务知识 |
| **LoRA（低秩适配）** | 只给实习生发一个小本子记笔记 | 训练时只修改模型里极少一部分参数（本项目只有 1.75%），不重写整个"脑子"，便宜又省显存 |
| **训练数据** | 培训教材 | 本项目用的是 ECD 数据集：淘宝真实客服对话记录 |
| **ChatML 格式** | 剧本格式 | 记录"谁说了什么"的标准格式（system/user/assistant 三种角色） |
| **GPU / 显存** | 发动机 / 油缸容量 | 训练模型靠显卡计算；显存（VRAM）决定一次能"吞下"多少数据 |
| **环境（conda）** | 实验台 | 一个独立的 Python 工作空间，避免不同项目互相打架 |
| **检查点（checkpoint）** | 游戏存档 | 每训练一段就保存一份模型状态，随时可以回退或试用 |
| **completion_only_loss** | 培训只练"客服怎么答" | 训练时只学习客服回复部分，不学习顾客说的话 |

**为什么微调要用 LoRA 而不是全量训练？**
因为完整训练一个大模型需要几十张高级显卡和几周时间。LoRA 只在模型原有基础上加一层"小补丁"，训练参数少、显存占用低，普通家用游戏显卡（比如你的 RTX 3050）也能跑。

---

## 2. 完整过程记录（时间线）

```mermaid
flowchart LR
    A[1. 查看项目<br/>了解它做什么] --> B[2. 克隆代码<br/>下载到本地]
    B --> C[3. 检查电脑<br/>GPU / conda 环境]
    C --> D[4. 创建 Python 环境<br/>qwen25]
    D --> E[5. 安装依赖包<br/>torch 等 27 个库]
    E --> F[6. 下载底座模型<br/>魔搭社区 988MB]
    F --> G[7. 冒烟测试<br/>3 条数据跑通全流程]
    G --> H[8. 正式训练<br/>1 万条数据 1 个 epoch]
    H --> I[9. 对比测试 + 对话体验]
    I --> J[10. 合并权重 + 对话实测]
```

### 第 1 步：查看项目（做什么的？）

- 项目地址：https://github.com/Luoyifeiandxs/qwen2.5-finetune
- 这是一个**开源教学项目**：用真实电商客服对话数据，把 Qwen2.5-0.5B 小模型微调成"客服机器人"。
- 项目自带完整流程：训练 → 验证 → 微调前后对比测试 → LLM 盲评 → 对话，所有结果保存在本地，互不覆盖。

### 第 2 步：克隆代码（把代码拿到自己电脑）

- 用 `git clone` 把仓库下载到本地目录 `D:\qwen2.5微调`。
- 下载后项目里有什么：

```
qwen2.5-finetune/
├── train.py              # 训练脚本（核心，训练+验证）
├── test.py               # 测试脚本（微调前后对比）
├── chat.py               # 对话脚本（和微调后的模型聊天）
├── llm_as_judge.py       # LLM 盲评脚本（可选，需要 API key）
├── merge_model.py        # 合并 LoRA 权重脚本
├── scripts/              # 数据转换、模型下载等工具
├── data/                 # 训练数据（ECD 电商客服 1万~3万条）
├── evaluation/           # 评估框架（指标计算）
└── models/               # 底座模型目录（本来为空，后续下载）
```

### 第 3 步：检查电脑（你的机器配置）

用命令查到了你的实际情况：

| 检查项 | 结果 | 说明 |
|---|---|---|
| GPU | **NVIDIA RTX 3050 Laptop，4GB 显存** | 够跑 0.5B 小模型，但比原作者（1660 SUPER 6GB）显存小，训练参数要调小 |
| miniconda | 装在 `D:\miniconda` | 管理 Python 环境的工具，但它不在 PATH 里，要用完整路径调用 |
| qwen25 环境 | **不存在** | 项目要求的专用环境需要自己创建 |

### 第 4 步：创建 Python 环境（搭实验台）

```powershell
D:\miniconda\Scripts\conda.exe create -n qwen25 python=3.10 -y
```

- `qwen25` 是一个独立的"房间"，里面装 Python 3.10，之后所有依赖都装在这里，不影响电脑上其他 Python。
- 之后每次使用前要先"进门"：`conda activate qwen25`

### 第 5 步：安装依赖（把工具放进房间）

- 安装清单在 `requirements.txt` 里，共 27 个 Python 库，核心几个：
  - **torch（PyTorch）**：AI 训练框架，最重要
  - **transformers**：加载/使用模型的官方库
  - **trl / peft**：微调工具库（LoRA 就是它实现的）
  - **datasets**：读取数据
  - **modelscope**：从魔搭社区下载模型的工具
- **踩坑事件**：清华镜像默认装的 torch 是 **CPU 版**（`torch.cuda.is_available()` 返回 False），等于发动机没接电！改用阿里云 PyTorch 镜像重装 `torch 2.5.1+cu121` 后，CUDA 正常（RTX 3050 被识别）。

### 第 6 步：下载底座模型（领取"实习生"）

```powershell
python scripts/download_model.py --source modelscope
```

- 从**魔搭社区**（ModelScope，国内访问快）下载 Qwen2.5-0.5B，共 11 个文件，其中模型权重 `model.safetensors` 988MB，总共约 1GB。
- 下载到 `models\Qwen2.5-0.5B`。
- 验证通过：词表 15.16 万，模型参数 4.94 亿，符合预期。

### 第 7 步：冒烟测试（先小规模试跑）

- 用 `data\sample` 里只有 **3 条数据**，跑 1 个 epoch（约 30 秒），验证全流程能通。
- 测试命令：

```powershell
python train.py --data_path ./data/sample/train.json --eval_data_path ./data/sample/eval.json --num_epochs 1 --batch_size 2
```

- 结果解读：
  - LoRA 生效：可训练参数 879 万，只占全部参数的 **1.75%**（这就是 LoRA 省显存的原因）
  - loss 从 1.525 降到 1.498（数值越小 = 模型预测越准）
  - token 准确率 0.605（下一词预测正确率 60.5%）
  - 输出目录：`output\sample_train_20260906_155028\`（每次训练独立命名，永不覆盖）

---

## 3. 每个环节在做什么（人话版）

1. **克隆代码** = 把别人写好的"培训教材 + 培训流程"拿到自己电脑上。
2. **建环境** = 腾出一间干净的房间，只放和这个项目有关的工具，避免和别的项目冲突。
3. **装依赖** = 把培训要用的工具（框架、库）搬进房间。其中 torch 相当于"发动机"，必须确认它认识显卡（CUDA）。
4. **下载模型** = 领取那个"通才实习生"（通用模型），它之后要接受客服培训。
5. **冒烟测试** = 先用 3 条题目演练一遍完整流程，确认"发动机能转、教材能读、能存档"，避免直接跑 2 小时正式训练才发现问题。
6. **正式训练（下一步）** = 用 1 万条真实客服对话正式培训，让模型真正学会客服话术。
7. **测试/对话（再下一步）** = 考试（对比微调前后回答质量）+ 实战演练（和它聊天看效果）。

---

## 4. 当前状态（进度清单）

| 项目 | 状态 | 说明 |
|---|---|---|
| 项目代码 | ✅ 完成 | `D:\qwen2.5微调` |
| conda 环境 | ✅ 完成 | `qwen25`（Python 3.10） |
| PyTorch | ✅ 完成 | 2.5.1+cu121，**CUDA 可用**（RTX 3050 已识别） |
| 依赖库 | ✅ 完成 | transformers 5.16.1 / trl 1.12.0 / peft 0.20.0 / bitsandbytes 0.50.2 |
| 底座模型 | ✅ 完成 | `models\Qwen2.5-0.5B`（魔搭下载，988MB） |
| 冒烟测试 | ✅ 完成 | `output\sample_train_20260906_155028\`（3 条数据跑通全流程） |
| 正式训练 | ✅ 完成 | `output\ecd_train_20260906_155252\`（1 epoch / 10k 数据 / 57 分钟 / eval_loss 2.43，详见 §7.1） |
| 对比测试 | ✅ 完成 | `output\ecd_test_20260906_171000\`（质量全线提升，端到端快约 8 倍，详见 §7.2） |
| 3-epoch 实验 | ✅ 完成 | `output\ecd_train_20260906_180655\`（4 小时 13 分，结论：**2-epoch 最优 checkpoint-5000**，第 3 轮过拟合，详见 §7.3） |
| 权重合并 | ✅ 完成 | `models\ecd_train_20260906_180655-merged`（1000MB 独立完整模型，加载/生成验证通过） |
| 对话实测 | ✅ 完成 | 8 题 5 对 3 错：短句话术达标，复杂意图会答偏/会编造，详见 §7.4 |
| 调参实验 | ⏳ 可选 | lr / lora_r / 数据量网格实验（§5.2） |
| 大模型盲评 | ⏳ 可选 | `llm_as_judge.py`（需 API key，§5.1） |

---

## 5. 后续路线图

训练 → 测试 → 合并 → 对话实测 已完成，主线闭环。后续按兴趣选择：

### 5.1 大模型盲评（自动化评估，推荐先做）

```powershell
set LLM_API_KEY=sk-xxx        # 阿里云百炼 DashScope key（国内推荐，默认 qwen-max 当裁判）
D:\miniconda\envs\qwen25\python.exe llm_as_judge.py
```

- 前提：先跑过 `test.py`（脚本自动读取最新 `generations_compare.json`）
- 盲评流程：随机打乱微调前/后到 A/B（消除位置偏差）→ 四维度打分（相关性 / 有帮助性 / 事实性 / 流畅性，1-5）→ 判胜者 → 还原身份
- 想评哪个模型就先跑对应 `test.py`，例如：`python test.py --model_path models\ecd_train_20260906_180655-merged --test_data_path ./data/ecd/test.jsonl --max_gen_samples 100`

### 5.2 调参实验（理解超参最有效的方式）

| 参数 | 当前值 | 实验方向 |
|---|---|---|
| learning_rate | 2e-4 | 1e-4（更稳）/ 3e-4（更快但可能震荡） |
| lora_r | 16 | 8（更省）/ 32（表达力更强） |
| 数据 | ecd 10k | 全量 3 万条 / ecd_food 食品品类 |
| epoch | 2 | 固定 2，别盲目加（§7.3 过拟合教训） |

实验方法：固定测试题集（§7.4 那 8 题）复测，看哪些"答错题"变对了。

### 5.3 换大底座（效果最明显）

- Qwen2.5-**1.5B**：权重约 1.5GB，能力明显更强；4GB 显存需 `--use_qlora`（4-bit 量化）才能跑
- 下载与配置以 `scripts/download_model.py --help` 为准

### 5.4 全量数据训练

README 提供 3 万条版本（`train_30k`），覆盖更广、盲区更少。

### 5.5 部署落地（学完微调后的方向）

- 已合并模型可直接被 vLLM / Ollama 等推理框架加载，或封装成 API 服务
- 固定测试题集 + 盲评脚本 = 后续每次改动的验收标准

## 6. 训练日志与评估指标详解（技术版）

> 字段出处：训练日志花括号行来自 transformers Trainer（loss / grad_norm / learning_rate）与 trl 1.12（entropy / mean_token_accuracy / num_tokens），对应 `train.py` 的 SFTConfig 与 trl sft_trainer.py；测试表字段来自 `test.py`。

### 6.1 训练指标（train.py 每 50 步一行）

| 指标 | 定义 | 正常表现与异常判断 |
|---|---|---|
| **loss** | 训练损失（交叉熵）：模型对"客服下一句"预测概率的负对数似然均值 | 持续下降 = 收敛正常；震荡不降 = 学习率过大或数据问题；变 NaN = 梯度爆炸 / lr 过大 |
| **grad_norm** | 梯度整体大小：反映这一步 loss 对全部可训练参数的总变化幅度（L2 范数） | 正常在几~几十间波动。**grad_norm 很大**：梯度爆炸，更新步幅巨大，训练震荡、loss 飞 NaN；**grad_norm 很小**：梯度消失，参数几乎不更新，模型学不动 |
| **learning_rate** | 学习率：参数更新的步长。本项目 cosine 调度：warmup 5% 从 0 线性升至峰值 2e-4，再余弦衰减至 ~0 | 过大：loss 震荡 / 发散；过小：收敛极慢，近乎不学 |
| **entropy** | 输出熵：模型预测下一 token 分布的不确定性（Shannon 熵，nats） | 高 = 输出近似均匀分布、对下一词没把握；低 = 分布集中、"笃定"；应随 loss 一起下降 |
| **mean_token_accuracy** | 下一词预测准确率：预测 token 与真实 token 一致的比例（-100 掩码位不计） | 持续上升 = 学到东西；停滞 = 模型容量或数据不足 |
| **num_tokens** | 累计处理的 token 数（所有 batch 的 token 之和） | 工作量度量，只增不减 |
| **epoch** | 训练进度：已跑完整训练集的轮数 | 1.0 = 完整一遍 |

### 6.2 验证指标（每 eval_steps=250 步一行）

| 指标 | 定义 | 判断 |
|---|---|---|
| **eval_loss** | 验证集交叉熵（模型未见过的数据上的 loss） | 与 train_loss 同步下降 = 泛化良好；**eval_loss 回升而 train_loss 继续降 = 过拟合**（记住训练集、泛化变差），应减少 epoch 或扩充数据 |
| **eval_entropy / eval_mean_token_accuracy** | 同训练对应指标，作用于验证集 | 同上 |

### 6.3 测试指标（test.py 对比表）

| 指标 | 定义 | 判断 |
|---|---|---|
| **exact_match** | 生成回复与标准答案逐字相等的比例 | 严格指标；客服短句场景个位数百分比即合理 |
| **bleu** | n-gram 重叠度（中文按字符切分、带平滑），精确率导向 | 衡量回复包含标准答案关键词/短语的程度；短句天然偏低 |
| **rouge1** | 单字重叠 F1，覆盖度导向 | 衡量"该说的点"覆盖多少 |
| **rougeL** | 最长公共子序列 F1，句子级骨架相似度 | 衡量回复与标准答案的结构重合度 |
| **ttft_ms** | 首 token 延迟：prefill + 1 步 decode 的耗时（毫秒） | 用户感知的响应速度，越低越快；LoRA 加载时偏高（适配器前向开销），合并权重后消除 |
| **prefill_tokens_per_sec** | 输入处理吞吐（prompt token/秒） | 越高越好 |
| **decode_tokens_per_sec** | 输出生成吞吐（token/秒） | 注意是"每 token"速度；**端到端总耗时 = 生成量 × 每 token 时间**，微调后生成量骤降（127→6.8 token），端到端反而更快 |

---

## 7. 结果记录（2026-09-06）

### 7.1 正式训练 · output\ecd_train_20260906_155252

| 项 | 值 |
|---|---|
| 命令 | `python train.py --num_epochs 1 --batch_size 4` |
| 数据 | train_10k.jsonl（10,000 条）/ eval.jsonl（4,788 条） |
| LoRA | r=16, α=32, dropout=0.05，可训练 8,798,208 参数（1.75%） |
| 时长 | 2500 步 / 57 分钟 / 1.37s·it⁻¹ |
| 训练 loss | 4.989 → ~2.4（全程均值 2.756） |
| 验证 loss | 3.133 → **2.430**（10 次评估单调下降） |
| 验证 token 准确率 | 0.408 → 0.511 |
| 产物 | `final/adapter_model.safetensors`（35MB）+ train_history.json / eval_results.json |

**结论**：收敛良好、无过拟合（eval_loss 全程下降且末尾仍在降），可继续训练 1~2 epoch。

### 7.2 对比测试 · output\ecd_test_20260906_171000（测试集 952 条，生成评估 100 条）

**质量指标（全线提升）**

| 指标 | 微调前 | 微调后 | 变化 |
|---|---|---|---|
| eval_loss | 6.2689 | 2.4881 | **-60.3%** |
| mean_token_accuracy | 0.2063 | 0.5051 | +0.2988 |
| eval_entropy | 4.9662 | 2.4295 | -2.5366 |
| exact_match | 0.0000 | 0.0600 | +0.0600 |
| bleu | 0.0015 | 0.0892 | **×59.5** |
| rouge1 / rougeL | 0.0023 | 0.0117 | ×5.1 |

**性能指标（每 token 变慢，端到端更快）**

| 指标 | 微调前 | 微调后 | 说明 |
|---|---|---|---|
| ttft_ms | 62.23 | 104.36 | LoRA 前向开销 |
| prefill_tokens_per_sec | 1723.3 | 1022.9 | 同上 |
| decode_tokens_per_sec | 20.31 | 10.54 | 同上 |
| 平均生成 token 数 | **127.2** | **6.8** | 微调前废话连篇顶到上限 |
| 100 条总耗时 | 10 分 36 秒 | **1 分 17 秒** | 端到端快约 8 倍 |

**人工抽查样例**（generations_compare.json）

| | 内容 |
|---|---|
| 顾客 | "那要怎样这也不行那也不行" |
| 标准答案 | "发邮政的哦" |
| 微调前 | "韵达不发的哦ativo-reply 回复韵达不发的哦ativo-reply..."（无限重复乱码） |
| 微调后 | "亲亲这边可以申请下退款哦" ✅ 像客服了 |

**结论**：微调成功——质量指标全线大涨；每 token 速度下降属 LoRA 固有开销，因生成量骤降 19 倍，端到端显著更快；`merge_model.py` 合并权重可消除该开销。

### 7.3 3-epoch 训练实验 · output\ecd_train_20260906_180655（2026-09-06 晚）

| 项 | 值 |
|---|---|
| 命令 | `python train.py --num_epochs 3 --batch_size 4` |
| 数据 | 同上（train_10k 10,000 条 / eval 4,788 条） |
| 时长 | 7500 步 / **4 小时 13 分**（每步 2.03s，比首轮 1.37s 慢，疑似笔记本散热降频） |
| 产物 | `checkpoint-2500 / 5000 / 7500` + `final` + train_history.json / eval_results.json |

**关键结论：2-epoch（step 5000）最优，第 3 轮过拟合**

| 观察点 | Epoch 1 → 2 | Epoch 3 |
|---|---|---|
| eval_loss | 2.523 → **2.349**（step 5000 全周期最低） | 5250 步跳涨到 2.548，之后 2.50~2.52 平台震荡 |
| 验证准确率 | 0.492 → 0.527（持续上升） | 卡死 0.526，不再进步 |
| 训练 loss | 2.72 → 1.94 | 猛降到 ~1.3（训练准确率 70%+） |
| 验证熵 | 2.45 → 2.10 | 骤降到 1.60（过度自信） |

**过拟合判据**：训练 loss 继续降 + 验证 loss 回升 + 验证熵骤降 = 模型开始背训练集而非学规律。**正式成果选用 `checkpoint-5000`**（eval_loss 2.349 / 准确率 0.527，两项都优于 1-epoch 的 2.430 / 0.511）。

> 备注：本轮与首轮在相同 step 的 eval_loss 略有差异（如 step 2500：2.523 vs 2.430），属 fp16 舍入等非确定性噪声（约 3%），不影响趋势结论。

### 7.4 对话实测（2026-09-06 晚 · 合并模型 chat.py）

模型：`models\ecd_train_20260906_180655-merged`（checkpoint-5000 合并版，temperature 0.6 / top_p 0.9 / 重复惩罚 1.15）

| 用户提问 | 模型回答 | 判定 |
|---|---|---|
| 你好，有客服吗 | 您好 | ✅ 合格 |
| 你们发什么快递？能发邮政吗 | 韵达哦 | ✅ 合格（真实话术） |
| 老板，便宜点呗，我买多件能不能优惠？ | 价格最低了 | ✅ 合格（砍价拒绝） |
| 拿 200 个什么价格？ | 您要多少 | ⚠️ 半对（先反问确认，显得机械） |
| 200 | 那这边拍下帮您改价 | ✅✅ 亮点（地道的淘宝改价流程） |
| 东西收到了不合适，能退货吗 | 好的 | ❌ 敷衍，无退货流程 |
| 你们有卖电视吗 | 有的呢 | ❌ 编造（训练集外瞎答） |
| 我不想要了，怎么申请退款？ | 亲看下有没有活动 | ❌ 意图答偏（高频词"活动"硬套） |

**结论**：5 对 3 错。模型学会了**短句话术与业务动作**（拍下改价、拒绝砍价、韵达/邮政），但 0.5B 底座 + 1 万条短数据的天花板明显：复杂意图会答偏、训练集外问题会编造。**这是预期内的定位，不是失败**——它适合"简短话术 FAQ"，不适合多轮智能客服。后续提升方向：换 1.5B 底座 / 全量 3 万条数据 / 调参复测。

### 7.5 LLM 盲评基线（2026-09-07 · 裁判 Qwen2.5-72B-Instruct 硅基流动）

| 维度 | 微调前 | 微调后 | 变化 |
|---|---|---|---|
| relevance 相关性 | 1.089 | 2.405 | +1.316 |
| helpfulness 有帮助性 | 1.013 | 2.127 | +1.114 |
| factuality 事实性 | 1.063 | 2.544 | +1.481 |
| fluency 流畅性 | 1.063 | 3.127 | +2.064 |

- 胜率：微调后 **92.4%** / 微调前 7.6% / 平局 0（100 条样本，79 条有效，21 条裁判输出未按 JSON 格式被丢弃）
- 产物：`output\ecd_test_20260907_101027\llm_judge_results.json`
- 命令：`llm_as_judge.py --api_base https://api.siliconflow.cn/v1 --api_key sk-xxx --judge_model Qwen/Qwen2.5-72B-Instruct`
- **解读**：相对提升碾压（对比对象是乱码重复的底座），绝对分仍低（2.1~3.1/5）——模型"会说人话"但"帮不上大忙"：**流畅性最好、有帮助性最弱**（回答太短、信息量不足）。与 test.py 指标（BLEU 0.108 / exact_match 8%）互相印证
- **判卷规律**（10 条明细观察）：简短确认类 4 分（"看见订单了么"→"看到了哦"）；信息咨询类 3 分（答得出但生硬）；复杂售后/抱怨类 1-2 分（只能挤出安抚短句或答非所问）
- **用途**：当前模型的**官方基线**。之后每次改动（全量数据 / 1.5B 底座 / 调参）重跑同一套测试+盲评对比此表

**阶段结论（2026-09-07）**：0.5B + 1 万条短数据，产出达到预期定位——"短句话术型客服"，**验收通过，暂不继续投入**。升级路线（全量 3 万条数据 / Qwen2.5-1.5B + QLoRA）保留在 §5，随时可重启。

---

## 8. 踩坑备忘录（下次直接看这里）

| 问题 | 原因 | 解决办法 |
|---|---|---|
| `torch.cuda.is_available()` 返回 False | 清华 pip 源装的是 CPU 版 torch | `pip install "torch==2.5.1+cu121" --index-url https://mirrors.aliyun.com/pytorch-wheels/cu121/` |
| `conda` 命令找不到 | miniconda 没加入 PATH | 用完整路径 `D:\miniconda\Scripts\conda.exe`，或手动把 `D:\miniconda\Scripts` 加进系统 PATH |
| 显存不足（OOM） | batch 太大 | 加 `--batch_size 4`，必要时开 `--use_qlora`（4-bit 量化加载） |
| 模型下载慢/失败 | huggingface 国内访问不稳 | 用魔搭：`python scripts/download_model.py --source modelscope` |
| `merge_model.py` 报 `Not a LoRA adapter` | 脚本 `--model_path` 默认值是**作者电脑路径**（不是自动找最新） | 必须显式传参：`python merge_model.py --model_path output\ecd_train_xxx\checkpoint-5000` |
| `conda activate` 报 `Invoke-Expression: Missing argument` | **conda 4.12（2021 老版）的 PowerShell 钩子与 PowerShell 7.6+ 不兼容**（PS 5.1 正常） | 项目命令直接用完整路径 `D:\miniconda\envs\qwen25\python.exe`；或升级 conda：`D:\miniconda\Scripts\conda.exe update -n base conda -y` 后重开终端 |
| `chat.py` 报 transformers ImportError（huggingface-hub 版本冲突） | 误用了**系统 Python 3.11**（`C:\Users\xou\AppData\Local\Programs\Python\Python311`），不是 qwen25 环境 | 项目命令一律用 `D:\miniconda\envs\qwen25\python.exe`，判断标准：提示符前缀 `(qwen25)` |
| bf16 报错 | 老显卡不支持 bf16 | 保持默认 `--fp16 True --bf16 False`（你的 RTX 3050 支持 bf16，但 fp16 更稳） |

---

## 9. 常用命令速查

```powershell
# 注：PS 7 下 conda 不可用，项目命令统一用完整路径（以下均可照抄）
D:\miniconda\envs\qwen25\python.exe train.py --num_epochs 2 --batch_size 4   # 训练（最优 2 轮）
D:\miniconda\envs\qwen25\python.exe test.py --test_data_path ./data/ecd/test.jsonl --max_gen_samples 100  # 对比测试
D:\miniconda\envs\qwen25\python.exe merge_model.py --model_path output\ecd_train_xxx\checkpoint-5000  # 合并 LoRA（必须传路径）
D:\miniconda\envs\qwen25\python.exe chat.py --model_path ./models/ecd_train_20260906_180655-merged  # 对话
D:\miniconda\envs\qwen25\python.exe llm_as_judge.py --api_base https://api.siliconflow.cn/v1 --api_key sk-xxx --judge_model Qwen/Qwen2.5-72B-Instruct  # 大模型盲评（硅基流动）
D:\miniconda\envs\qwen25\python.exe scripts/convert_ecd_to_chatml.py  # 转换自己的客服数据
```

---

## 10. 微调过程与超参数设置（面试讲述版 · 2026-09-08）

### 10.1 一句话本质

**微调不是"教模型新知识"，而是"调整行为风格"**——底座 Qwen2.5-0.5B 已经会中文、会对话，微调是用 1 万条真实客服对话让它"学会像电商客服那样说话"。

### 10.2 微调六步过程

```
① 数据准备 → ② LoRA 注入 → ③ 训练循环 → ④ 训练中验证 → ⑤ 正式评估 → ⑥ 合并部署
```

| 步骤 | 做什么 | 关键产出 |
|---|---|---|
| ① 数据准备 | ECD 原始 100 万行 → 清洗转换 → **1 万条 ChatML**（train）+ 4788（eval）+ 952（test） | `data/ecd/train_10k.jsonl` 等 |
| ② LoRA 注入 | 冻结底座 4.94 亿参数（98.25%），只新增训练 **879 万低秩矩阵**（1.75%） | 显存 ~1GB 可训，checkpoint 仅 35MB |
| ③ 训练循环 | 输入对话 → 前向 → **只在 assistant 回复段算 loss**（user 段 mask）→ 反向 → 只更新 LoRA 矩阵 | 每 epoch 2500 步，`train_history.json` |
| ④ 训练中验证 | 每 250 步在 eval 集算 loss/准确率/熵 → **数据驱动选 checkpoint** | 2-epoch（step 5000）eval_loss 最低 2.349 |
| ⑤ 正式评估 | 独立 test 集 952 条：三层指标 + LLM 盲评 | eval_loss 6.27→2.38；胜率 92.4% |
| ⑥ 合并部署 | W' = W + (α/r)·BA 数学等价合并 → 完整模型 ~1GB | `models/xxx-merged/`，chat.py 可对话 |

### 10.3 核心逻辑（为什么 LoRA、为什么这么训）

- **为什么 LoRA 可行**：微调本质是调整权重 ΔW。LoRA 假设单风格任务的 ΔW 有效信息量很低（低秩），可用两个小矩阵近似 `ΔW ≈ B·A`。冻结主参数只训小矩阵 → 显存/时间/体积缩小 20-50 倍，效果接近全量微调。
- **训练循环最关键一步**：loss 只在 **assistant 回复段**计算（user 问题部分 mask 掉）。SFT 目标是"看到顾客这句 → 学会回客服那句"，不是让模型复述问题。这一步决定模型学的是"对话行为"而非"文本模仿"。
- **合并是数学无损**：`W' = W + (α/r)·BA` 把增量加回主权重，与带 LoRA 推理数学完全等价。合并前后行为一致，唯一区别是推理不再跑低秩运算（速度恢复原生）。

### 10.4 超参数设置（每个为什么）

| 参数 | 本项目值 | 为什么这么设 |
|---|---|---|
| `num_epochs` | **2** | 不是拍脑袋：3-epoch 实验显示第 3 轮 eval_loss 从 2.349 跳涨到 2.55+、验证熵骤降（过拟合），数据驱动定 2 |
| `batch_size` | 4 | **显存约束**：4GB 卡 + 1024 序列长度，默认 16 会 OOM；batch 小梯度噪声大，但 LoRA 只训新参数、对噪声不敏感 |
| `learning_rate` | 2e-4 | LoRA 标准区间 1e-4~3e-4。只训新参数，学习率可比全量微调（1e-5）大一个量级 |
| `lr_scheduler` | cosine + warmup 5% | warmup 开头"小步走稳"避免震荡，cosine 后期平滑收敛到低 loss 区域 |
| `weight_decay` | 0.01 | 通用防过拟合，克制参数过大 |
| `max_seq_length` | 1024 | ECD 最长样本约 1000 token：**够用**（不截断丢信息）且**省显存**（序列越长显存暴涨） |
| `lora_r` | 16 | rank=表达能力。单一风格任务（客服话术）16 够；复杂任务才需 32-64 |
| `lora_alpha` | 32 | 缩放比 α/r=2，LoRA 论文常用配置（实际效果对 α/r 不敏感） |
| `lora_dropout` | 0.05 | 适配器上的正则，防止只记住训练样本 |
| `fp16` | True | RTX 3050 **不支持 bf16**（硬性约束，开了直接报错）；fp16 半精度省显存加速 |
| `gradient_checkpointing` | True | 前向不存中间激活、反向时重算——省显存换约 30% 速度，4GB 卡必开 |

**规律总结**：训练类参数（lr/调度/warmup）走 LoRA 标准配方；资源类参数（batch/序列长/fp16/检查点）由显卡决定；结构类参数（r/α/dropout）由任务复杂度决定；epoch 靠实验数据定。

### 10.5 数据样本来源（过程第一步的细节）

- **ECD**（E-commerce Dialogue Dataset）：淘宝**真实客服对话**，约 100 万行，研究社区常用电商客服基准
- 原始格式为**检索式**：`label \t 对话轮次 \t 候选回复`（label=1 表示该候选回复被真实采用）
- `convert_ecd_to_chatml.py` 转换 4 条规则：①只取 label=1 正样本（负样本会教坏模型）②去分词空格恢复连续中文 ③奇偶轮次分配 user（顾客）/assistant（客服）④附加电商客服 system prompt
- **数据划分**：随机抽 1 万训练 + 4788 验证 + 952 测试（**模型从未见过**，最后验收用）
- **为什么 1 万不是 3 万**：先跑通全链路 + 控制实验时间（1 万 × 2 epochs 约 3 小时）；全量 3 万条作为升级路线（§5）

---

*本文档随学习进度持续更新。面试讲述版（§10）已完成；后续更新点：ComfyUI 工作流、服务化部署、全量 3 万条实验。*
